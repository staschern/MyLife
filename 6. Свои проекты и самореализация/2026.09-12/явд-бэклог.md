# Бэклог ЯВД (yavdele.com)

Ведёт: агент «Архитектор ЯВД» (Ольга Жукова), сфера «Свои проекты и самореализация».
При еженедельном обращении (по плану — раз в выходные) предлагает один конкретный
следующий шаг с учётом приоритета и доступного времени. Раз в месяц — сводка прогресса.

## Полный бэклог (на 2026.09, без порядка приоритета)

- Редизайн страниц.
- Адаптивная вёрстка под мобильные.
- Устранение недочётов/неточностей.
- Перенос ручных xls-расчётов по капиталу/инвестициям в механизмы сайта (раздел
  «Финансы»).
- Геймификация с советниками по сферам жизни (фото, всплывающие напоминания и мудрые
  мысли каждый день — почта/Telegram и другие каналы).
- Обучающий квест для новых пользователей.
- Тренажёр памяти по карточкам для изучения языков (иероглифы, история ответов, подбор
  вопросов по частотности ошибок).
- MCP-интеграция для работы с ИИ-моделями.
- Двухфакторная аутентификация.
- Публикация страниц для сторонних пользователей.
- **Раздел планирования дня/недели** (добавлено 12.09.2026, пользователь считает — не
  было в бэклоге): просмотр всех 7 дней недели с почасовым расписанием (как в HTML,
  который сейчас готовит Артём — см. «В работе»), ручное редактирование прямо на сайте,
  копирование шаблона существующего дня при создании нового, быстрая загрузка недельного
  расписания через JSON на заданные дни.

## В работе

- **Раздел планирования дня/недели** (с 12.09.2026, выбран пользователем как первый
  шаг вместо переноса xls-расчётов — тот остаётся в бэклоге на потом). Источник
  недельного расписания/JSON подтверждён — **Артём** («Конструктор дня»), не Роман
  (была путаница имён, снята).

  **Доступ к репозиторию `staschern/yavdele2`** подключён 12.09.2026 (push), рабочая
  ветка — `claude/yii2-project-setup-dsPYD` (единственная существующая в репозитории).

  **Важная находка (12.09.2026, перед первой привязкой гита на сервере) — в git
  закоммичены конфиги с боевыми/секретными данными:**
  - `common/config/main-local.php` — DB-подключение (в репозитории значения-плейсхолдеры
    `yavdele`/`yavdele2pass`/`yavdele2`, но на живом сервере наверняка свои реальные).
  - `frontend/config/main-local.php` — `cookieValidationKey` (секрет для валидации
    cookie — при замене разлогинит всех пользователей).
  При первой привязке гита к уже работающему проекту (сейчас не связан с гитом вообще)
  наивный `git checkout`/`pull` поверх живой папки затрёт эти 2 файла плейсхолдерами и
  сломает подключение к БД. План: после `git init` в живой папке защитить именно эти
  2 файла через `git update-index --skip-worktree`, чтобы будущие pull'ы их не трогали.
  PHP на сервере — 7.4.3 (подтверждено пользователем); код Этапа 1 проверен вручную —
  синтаксиса PHP 8+ (match, ?->, enum, promoted properties, именованные аргументы) не
  использовано, конфликта с composer.json (`"php": ">=5.4.0"`) нет.

  **Исправлено в самом репозитории (коммит `a89a1d4`):** 6 файлов `*-local.php`
  (`common/config`, `frontend/config` — main/params/test) убраны из индекса гита
  (`git rm --cached`, на диске остались) и добавлены в `.gitignore` — так эти файлы
  никогда не попадут под git на сервере при первой привязке. Путь проекта на сервере:
  `/var/www/www-root/data/www/yavdele.com`. PHP-команда — просто `php`. `vendor/` уже
  стоит. Пользователь уже сделал полный бэкап (архив файлов + дамп БД) перед началом.

  **Этапы:**
  1. ✅ Просмотр недели без редактирования (готово 12.09.2026, см. «Сделано»).
  2. ✅ Ручное добавление/редактирование/удаление блоков расписания (готово 12.09.2026,
     см. «Сделано») — задеплоено на сервер и проверено.
  3. ⏳ «Скопировать день» — перенос всех блоков одной даты на другую.
  4. ⏳ Импорт недельного расписания через JSON от Артёма.

  **Проверено на бою (12.09.2026):** страница «Неделя» открылась и корректно показала
  раскраску дней (Пн 07.09 Образование, Вт 08.09 Финансы, Ср 09.09 Яркость, Чт 10.09
  Общение, Пт 11.09 Система, Сб 12.09 Образование) — совпадает с `../../1. Система и
  порядок/цвета-дней.md`, включая повтор Образования 07.09/12.09 из-за 8-дневного цикла.
  Побочно нашли и накатили 4 старые неприменённые миграции (`reminder`,
  `reminder_schedule`, `object`) — незавершённые старые доработки пользователя, решено
  не трогать сейчас, доработать отдельно при необходимости.

## Сделано

- **12.09.2026 — Этап 1: раздел «Неделя», только просмотр.**
  Коммит [`8b85ade`](https://github.com/staschern/yavdele2/commit/8b85ade) в ветку
  `claude/yii2-project-setup-dsPYD`.

  **Файлы:**
  - `console/migrations/m260912_070000_create_schedule_block_table.php` (новый) —
    таблица `schedule_block`: дата, `start_minutes`/`end_minutes` (минуты от полуночи),
    заголовок, опциональная сфера.
  - `common/models/goal/ScheduleBlock.php` (новый) — модель + выборки блоков за
    период/по дате.
  - `frontend/controllers/GoalController.php` (изменён) — добавлен `actionWeek()`:
    строит текущую неделю (Пн-Вс), навигация пред./след. неделя через `?n=`, подтягивает
    специализацию дня (`Calendar`, тот же механизм, что цвет дня в календаре) и блоки
    расписания.
  - `frontend/views/goal/week.php` (новый) — 7 карточек-дней с цветом сферы дня и
    списком блоков/пустым состоянием, в стиле существующих страниц
    (`window`/`window-border`).
  - `frontend/views/layouts/main.php` (изменён) — пункт меню «Неделя» добавлен в
    разделе «Система» сразу после «Календарь».

  Все файлы прошли `php -l` (проверка синтаксиса) перед коммитом.

  **Инструкция для сервера** (получить изменения и применить миграцию):
  ```bash
  cd /путь/к/проекту/yavdele2
  git pull origin claude/yii2-project-setup-dsPYD
  php yii migrate --interactive=0
  php yii cache/flush-all
  ```
  Проверка: открыть «Система → Неделя» в меню — должна показаться текущая неделя,
  7 дней с цветом специализации (если она проставлена в календаре) и пустым списком
  блоков (блоков пока негде добавить — это Этап 2).

- **12.09.2026 — Этап 2: добавление/редактирование/удаление блоков расписания.**
  Коммит [`17afcbc`](https://github.com/staschern/yavdele2/commit/17afcbc) в ветку
  `claude/yii2-project-setup-dsPYD`. Новых миграций нет.

  **Файлы:**
  - `frontend/controllers/GoalController.php` (изменён, +67 строк) —
    `actionScheduleBlockSave()` (создаёт новый блок или обновляет существующий, если
    id принадлежит текущему пользователю) и `actionScheduleBlockDelete()` (мягкое
    удаление, `is_deleted=1`). Работают через обычный POST + редирект на ту же неделю
    (`?n=`) — без AJAX, для надёжности первой версии.
  - `frontend/views/goal/week.php` (изменён, +49/-9 строк) — под каждым блоком форма
    редактирования (время начала/конца, заголовок, «Сохранить») и форма «Удалить» с
    подтверждением; внизу каждой карточки дня — форма добавления нового блока.

  **Инструкция для сервера** (код без БД-изменений, только `git pull`):
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  (Если к этому моменту накопились ещё чужие правки в этих двух файлах — сначала
  `git --no-pager diff --stat -- frontend/controllers/GoalController.php
  frontend/views/goal/week.php`, как в Этапе 1, и свериться перед checkout.)

  Проверка: на странице «Неделя» — добавить блок (время + заголовок) в любой день,
  сохранить, отредактировать его, затем удалить.

- **12.09.2026 — редизайн: 3-колоночная сетка вместо 7 карточек, строка выбора дня.**
  Пользователь показал скриншот с 7 днями горизонтальным скроллом — выглядело криво;
  попросил разместить дни в 3 колонки (по образцу расписания Артёма, у него 2), убрать
  горизонтальный скролл (только вертикальный), в ряду выводить по центру выбранный день
  (по умолчанию сегодня), слева — предыдущий день, справа — следующий, плюс сверху строку
  выбора любого дня текущей (относительно выбранного) недели, где выбранный день
  подсвечен и недоступен для повторного клика. Коммит
  [`4204239`](https://github.com/staschern/yavdele2/commit/4204239) в ветку
  `claude/yii2-project-setup-dsPYD`. Новых миграций нет, схема БД не менялась.

  **Файлы:**
  - `frontend/controllers/GoalController.php` (изменён) — `actionWeek()` переписан:
    вместо всей недели строит 3-дневное окно (вчера/выбранный день/завтра) вокруг
    `$selectedDate` (из `?n=` или сегодня); отдельно вычисляет `$weekTabDates` (Пн-Вс
    недели выбранного дня) для строки выбора дня; диапазон запроса специализации/блоков
    расширен на 1 день с каждой стороны на случай, если выбранный день — граничный
    (Пн/Вс) и окно частично уходит в соседнюю неделю.
  - `frontend/views/goal/week.php` (изменён) — добавлена строка выбора дня сверху
    (текущий выбранный — подсвеченный `<span>`, остальные — ссылки `?n=`), сетка на
    CSS Grid (`repeat(3, 1fr)` от 820px и шире, 1 колонка на узких экранах вместо
    горизонтального скролла), центральная карточка (выбранный день) визуально выделена
    рамкой; формы редактирования/удаления/добавления блоков — без изменений в логике,
    только сохранены под новую вёрстку.

  **Инструкция для сервера** (код без БД-изменений, только обновление файлов из гита —
  **важно сначала сделать fetch+reset**, иначе `git checkout --` возьмёт устаревшие
  версии из локального индекса, как уже было при Этапе 2):
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  После этого — жёстко обновить страницу в браузере (Ctrl+F5/Cmd+Shift+R), т.к. могли
  закэшироваться старые CSS/вёрстка.

  Проверка: на странице «Неделя» — 3 колонки дней (не горизонтальный скролл), выбранный
  день (по умолчанию сегодня) — по центру и визуально выделен, слева вчерашний
  относительно него, справа завтрашний; сверху строка дней текущей недели — клик по
  любому дню недели центрирует сетку на нём (слева/справа — соседние относительно него
  дни), сам выбранный день в строке недоступен для повторного клика.

- **12.09.2026 — доработка: скрытое редактирование строк + исправление тёмной темы.**
  Пользователь прислал новые скриншоты (в т.ч. с включённой тёмной темой сайта): поля
  редактирования были видны сразу во всех строках (криво, много кнопок), а чёрный текст
  на насыщенном цвете сферы дня (например, синий «Образование») был не виден в тёмной
  теме — как и белые фоны полей ввода, не подстроенные под тёмную тему. Коммит
  [`43f890a`](https://github.com/staschern/yavdele2/commit/43f890a) в ветку
  `claude/yii2-project-setup-dsPYD`. Только `week.php`, БД/миграции не менялись.

  **Файлы:**
  - `frontend/views/goal/week.php` (изменён) — каждый блок расписания теперь по
    умолчанию отображается строкой «время | название» без полей ввода; редактирование
    (время начала/конца + название, Сохранить/Отмена) открывается только для одной
    выбранной строки — по клику на иконку карандаша (`glyphicon-pencil`, тот же элемент,
    что на странице профиля пользователя); открытие новой строки закрывает ранее
    открытую. Добавление блока — по ссылке «+ Добавить», тоже раскрывает форму только
    для этой строки. Удаление — иконкой (`glyphicon-remove`) вместо отдельной кнопки.
    Все жёстко заданные цвета (белый фон, `#333`/`#eee`/`#ccc` и т.п.) заменены на
    CSS-переменные темы проекта (`var(--text)`, `var(--surface)`, `var(--border)`,
    `var(--primary)` из `tokens.css`) — это касается и полей ввода времени/названия
    внутри формы редактирования, и выделения выбранного дня/таба недели.

  **Инструкция для сервера** (снова fetch+reset перед checkout, код без БД-изменений):
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  Затем — жёсткое обновление страницы в браузере (Ctrl+F5).

  Проверка: на странице «Неделя» (в т.ч. с включённой тёмной темой) — строки блоков
  показывают только время и название, без полей ввода; клик на карандаш открывает
  редактирование именно этой строки (остальные закрываются), время/название/цвета
  таба недели и выделения читаемы на любой сфере дня и в любой теме.

## Сводка прогресса (раз в месяц)

_Первая сводка — через месяц после старта работы с агентом._
