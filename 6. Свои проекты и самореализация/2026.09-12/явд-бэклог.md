# Бэклог ЯВД (yavdele.com)

Ведёт: агент «Архитектор ЯВД» (Ольга Жукова), сфера «Свои проекты и самореализация».
При еженедельном обращении (по плану — раз в выходные) предлагает один конкретный
следующий шаг с учётом приоритета и доступного времени. Раз в месяц — сводка прогресса.

## Полный бэклог (на 2026.09, без порядка приоритета)

- Редизайн страниц.
- Адаптивная вёрстка под мобильные.
- Устранение недочётов/неточностей.
- Перенос ручных xls-расчётов по капиталу/инвестициям в механизмы сайта (раздел
  «Финансы»).
- Геймификация с советниками по сферам жизни (фото, всплывающие напоминания и мудрые
  мысли каждый день — почта/Telegram и другие каналы).
- Обучающий квест для новых пользователей.
- Тренажёр памяти по карточкам для изучения языков (иероглифы, история ответов, подбор
  вопросов по частотности ошибок).
- MCP-интеграция для работы с ИИ-моделями.
- Двухфакторная аутентификация.
- Публикация страниц для сторонних пользователей.
- **Раздел планирования дня/недели** (добавлено 12.09.2026, пользователь считает — не
  было в бэклоге): просмотр всех 7 дней недели с почасовым расписанием (как в HTML,
  который сейчас готовит Артём — см. «В работе»), ручное редактирование прямо на сайте,
  копирование шаблона существующего дня при создании нового, быстрая загрузка недельного
  расписания через JSON на заданные дни.

## В работе

- **Раздел планирования дня/недели** (с 12.09.2026, выбран пользователем как первый
  шаг вместо переноса xls-расчётов — тот остаётся в бэклоге на потом). Источник
  недельного расписания/JSON подтверждён — **Артём** («Конструктор дня»), не Роман
  (была путаница имён, снята).

  **Доступ к репозиторию `staschern/yavdele2`** подключён 12.09.2026 (push), рабочая
  ветка — `claude/yii2-project-setup-dsPYD` (единственная существующая в репозитории).

  **Важная находка (12.09.2026, перед первой привязкой гита на сервере) — в git
  закоммичены конфиги с боевыми/секретными данными:**
  - `common/config/main-local.php` — DB-подключение (в репозитории значения-плейсхолдеры
    `yavdele`/`yavdele2pass`/`yavdele2`, но на живом сервере наверняка свои реальные).
  - `frontend/config/main-local.php` — `cookieValidationKey` (секрет для валидации
    cookie — при замене разлогинит всех пользователей).
  При первой привязке гита к уже работающему проекту (сейчас не связан с гитом вообще)
  наивный `git checkout`/`pull` поверх живой папки затрёт эти 2 файла плейсхолдерами и
  сломает подключение к БД. План: после `git init` в живой папке защитить именно эти
  2 файла через `git update-index --skip-worktree`, чтобы будущие pull'ы их не трогали.
  PHP на сервере — 7.4.3 (подтверждено пользователем); код Этапа 1 проверен вручную —
  синтаксиса PHP 8+ (match, ?->, enum, promoted properties, именованные аргументы) не
  использовано, конфликта с composer.json (`"php": ">=5.4.0"`) нет.

  **Исправлено в самом репозитории (коммит `a89a1d4`):** 6 файлов `*-local.php`
  (`common/config`, `frontend/config` — main/params/test) убраны из индекса гита
  (`git rm --cached`, на диске остались) и добавлены в `.gitignore` — так эти файлы
  никогда не попадут под git на сервере при первой привязке. Путь проекта на сервере:
  `/var/www/www-root/data/www/yavdele.com`. PHP-команда — просто `php`. `vendor/` уже
  стоит. Пользователь уже сделал полный бэкап (архив файлов + дамп БД) перед началом.

  **Этапы:**
  1. ✅ Просмотр недели без редактирования (готово 12.09.2026, см. «Сделано»).
  2. ✅ Ручное добавление/редактирование/удаление блоков расписания (готово 12.09.2026,
     см. «Сделано») — задеплоено на сервер и проверено.
  3. ✅ «Скопировать день» — перенос всех блоков одной даты на другую (готово
     12.09.2026, см. «Сделано»).
  4. ✅ Альтернативные варианты дня + быстрый флажок «сделано» (готово 12.09.2026,
     см. «Сделано»).
  5. ✅ Импорт недельного расписания через JSON от Артёма (готово 12.09.2026,
     см. «Сделано»).

  **Версионирование (введено 12.09.2026, правило от пользователя):** версия сайта
  показывается в подвале, формат `v.A.B.C.D`. `A` меняется только по прямому указанию
  пользователя. `B` — номер триместра. `C` — новый механизм. `D` — новая функциональность
  в рамках существующего механизма. **После каждой доработки — обязательно напоминать
  пользователю о возможном обновлении версии**, если сам не поднял тему. Первое значение
  — `v.1.7.1.1`, установлено пользователем 12.09.2026 вместе со стартом Этапа 4; в рамках
  той же доработки версия больше не менялась (по прямому указанию).

  **Проверено на бою (12.09.2026):** страница «Неделя» открылась и корректно показала
  раскраску дней (Пн 07.09 Образование, Вт 08.09 Финансы, Ср 09.09 Яркость, Чт 10.09
  Общение, Пт 11.09 Система, Сб 12.09 Образование) — совпадает с `../../1. Система и
  порядок/цвета-дней.md`, включая повтор Образования 07.09/12.09 из-за 8-дневного цикла.
  Побочно нашли и накатили 4 старые неприменённые миграции (`reminder`,
  `reminder_schedule`, `object`) — незавершённые старые доработки пользователя, решено
  не трогать сейчас, доработать отдельно при необходимости.

## Сделано

- **12.09.2026 — Этап 1: раздел «Неделя», только просмотр.**
  Коммит [`8b85ade`](https://github.com/staschern/yavdele2/commit/8b85ade) в ветку
  `claude/yii2-project-setup-dsPYD`.

  **Файлы:**
  - `console/migrations/m260912_070000_create_schedule_block_table.php` (новый) —
    таблица `schedule_block`: дата, `start_minutes`/`end_minutes` (минуты от полуночи),
    заголовок, опциональная сфера.
  - `common/models/goal/ScheduleBlock.php` (новый) — модель + выборки блоков за
    период/по дате.
  - `frontend/controllers/GoalController.php` (изменён) — добавлен `actionWeek()`:
    строит текущую неделю (Пн-Вс), навигация пред./след. неделя через `?n=`, подтягивает
    специализацию дня (`Calendar`, тот же механизм, что цвет дня в календаре) и блоки
    расписания.
  - `frontend/views/goal/week.php` (новый) — 7 карточек-дней с цветом сферы дня и
    списком блоков/пустым состоянием, в стиле существующих страниц
    (`window`/`window-border`).
  - `frontend/views/layouts/main.php` (изменён) — пункт меню «Неделя» добавлен в
    разделе «Система» сразу после «Календарь».

  Все файлы прошли `php -l` (проверка синтаксиса) перед коммитом.

  **Инструкция для сервера** (получить изменения и применить миграцию):
  ```bash
  cd /путь/к/проекту/yavdele2
  git pull origin claude/yii2-project-setup-dsPYD
  php yii migrate --interactive=0
  php yii cache/flush-all
  ```
  Проверка: открыть «Система → Неделя» в меню — должна показаться текущая неделя,
  7 дней с цветом специализации (если она проставлена в календаре) и пустым списком
  блоков (блоков пока негде добавить — это Этап 2).

- **12.09.2026 — Этап 2: добавление/редактирование/удаление блоков расписания.**
  Коммит [`17afcbc`](https://github.com/staschern/yavdele2/commit/17afcbc) в ветку
  `claude/yii2-project-setup-dsPYD`. Новых миграций нет.

  **Файлы:**
  - `frontend/controllers/GoalController.php` (изменён, +67 строк) —
    `actionScheduleBlockSave()` (создаёт новый блок или обновляет существующий, если
    id принадлежит текущему пользователю) и `actionScheduleBlockDelete()` (мягкое
    удаление, `is_deleted=1`). Работают через обычный POST + редирект на ту же неделю
    (`?n=`) — без AJAX, для надёжности первой версии.
  - `frontend/views/goal/week.php` (изменён, +49/-9 строк) — под каждым блоком форма
    редактирования (время начала/конца, заголовок, «Сохранить») и форма «Удалить» с
    подтверждением; внизу каждой карточки дня — форма добавления нового блока.

  **Инструкция для сервера** (код без БД-изменений, только `git pull`):
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  (Если к этому моменту накопились ещё чужие правки в этих двух файлах — сначала
  `git --no-pager diff --stat -- frontend/controllers/GoalController.php
  frontend/views/goal/week.php`, как в Этапе 1, и свериться перед checkout.)

  Проверка: на странице «Неделя» — добавить блок (время + заголовок) в любой день,
  сохранить, отредактировать его, затем удалить.

- **12.09.2026 — редизайн: 3-колоночная сетка вместо 7 карточек, строка выбора дня.**
  Пользователь показал скриншот с 7 днями горизонтальным скроллом — выглядело криво;
  попросил разместить дни в 3 колонки (по образцу расписания Артёма, у него 2), убрать
  горизонтальный скролл (только вертикальный), в ряду выводить по центру выбранный день
  (по умолчанию сегодня), слева — предыдущий день, справа — следующий, плюс сверху строку
  выбора любого дня текущей (относительно выбранного) недели, где выбранный день
  подсвечен и недоступен для повторного клика. Коммит
  [`4204239`](https://github.com/staschern/yavdele2/commit/4204239) в ветку
  `claude/yii2-project-setup-dsPYD`. Новых миграций нет, схема БД не менялась.

  **Файлы:**
  - `frontend/controllers/GoalController.php` (изменён) — `actionWeek()` переписан:
    вместо всей недели строит 3-дневное окно (вчера/выбранный день/завтра) вокруг
    `$selectedDate` (из `?n=` или сегодня); отдельно вычисляет `$weekTabDates` (Пн-Вс
    недели выбранного дня) для строки выбора дня; диапазон запроса специализации/блоков
    расширен на 1 день с каждой стороны на случай, если выбранный день — граничный
    (Пн/Вс) и окно частично уходит в соседнюю неделю.
  - `frontend/views/goal/week.php` (изменён) — добавлена строка выбора дня сверху
    (текущий выбранный — подсвеченный `<span>`, остальные — ссылки `?n=`), сетка на
    CSS Grid (`repeat(3, 1fr)` от 820px и шире, 1 колонка на узких экранах вместо
    горизонтального скролла), центральная карточка (выбранный день) визуально выделена
    рамкой; формы редактирования/удаления/добавления блоков — без изменений в логике,
    только сохранены под новую вёрстку.

  **Инструкция для сервера** (код без БД-изменений, только обновление файлов из гита —
  **важно сначала сделать fetch+reset**, иначе `git checkout --` возьмёт устаревшие
  версии из локального индекса, как уже было при Этапе 2):
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  После этого — жёстко обновить страницу в браузере (Ctrl+F5/Cmd+Shift+R), т.к. могли
  закэшироваться старые CSS/вёрстка.

  Проверка: на странице «Неделя» — 3 колонки дней (не горизонтальный скролл), выбранный
  день (по умолчанию сегодня) — по центру и визуально выделен, слева вчерашний
  относительно него, справа завтрашний; сверху строка дней текущей недели — клик по
  любому дню недели центрирует сетку на нём (слева/справа — соседние относительно него
  дни), сам выбранный день в строке недоступен для повторного клика.

- **12.09.2026 — доработка: скрытое редактирование строк + исправление тёмной темы.**
  Пользователь прислал новые скриншоты (в т.ч. с включённой тёмной темой сайта): поля
  редактирования были видны сразу во всех строках (криво, много кнопок), а чёрный текст
  на насыщенном цвете сферы дня (например, синий «Образование») был не виден в тёмной
  теме — как и белые фоны полей ввода, не подстроенные под тёмную тему. Коммит
  [`43f890a`](https://github.com/staschern/yavdele2/commit/43f890a) в ветку
  `claude/yii2-project-setup-dsPYD`. Только `week.php`, БД/миграции не менялись.

  **Файлы:**
  - `frontend/views/goal/week.php` (изменён) — каждый блок расписания теперь по
    умолчанию отображается строкой «время | название» без полей ввода; редактирование
    (время начала/конца + название, Сохранить/Отмена) открывается только для одной
    выбранной строки — по клику на иконку карандаша (`glyphicon-pencil`, тот же элемент,
    что на странице профиля пользователя); открытие новой строки закрывает ранее
    открытую. Добавление блока — по ссылке «+ Добавить», тоже раскрывает форму только
    для этой строки. Удаление — иконкой (`glyphicon-remove`) вместо отдельной кнопки.
    Все жёстко заданные цвета (белый фон, `#333`/`#eee`/`#ccc` и т.п.) заменены на
    CSS-переменные темы проекта (`var(--text)`, `var(--surface)`, `var(--border)`,
    `var(--primary)` из `tokens.css`) — это касается и полей ввода времени/названия
    внутри формы редактирования, и выделения выбранного дня/таба недели.

  **Инструкция для сервера** (снова fetch+reset перед checkout, код без БД-изменений):
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  Затем — жёсткое обновление страницы в браузере (Ctrl+F5).

  Проверка: на странице «Неделя» (в т.ч. с включённой тёмной темой) — строки блоков
  показывают только время и название, без полей ввода; клик на карандаш открывает
  редактирование именно этой строки (остальные закрываются), время/название/цвета
  таба недели и выделения читаемы на любой сфере дня и в любой теме.

- **12.09.2026 — Этап 3: «Скопировать день».**
  Уточнил у пользователя перед разработкой (по правилу «не гадать»): (1) как выбирать
  день-назначение — кнопка на карточке дня + поле даты (а не отдельная форма над
  сеткой); (2) что делать, если в дне-назначении уже есть блоки — спросить
  подтверждение; (3) что происходит после подтверждения — заменить (старые блоки
  удаляются, вставляется копия дня-источника), а не добавить к существующим. Коммит
  [`9a976ae`](https://github.com/staschern/yavdele2/commit/9a976ae) в ветку
  `claude/yii2-project-setup-dsPYD`. Только код, миграций нет.

  **Файлы:**
  - `frontend/controllers/GoalController.php` (изменён) — новый `actionScheduleBlockCopyDay()`:
    если у дня-назначения нет блоков (или пришёл `confirmed=1`) — мягко удаляет старые
    блоки назначения и создаёт копии блоков дня-источника (время, заголовок, сфера);
    если блоки есть и подтверждения ещё не было — рендерит страницу подтверждения.
  - `frontend/views/goal/copy-confirm.php` (новый) — страница подтверждения: количество
    существующих блоков в дне-назначении, кнопки «Да, заменить»/«Отмена».
  - `frontend/views/goal/week.php` (изменён) — на каждой карточке дня добавлена ссылка
    «Скопировать в...», раскрывающая поле выбора даты-назначения (тот же паттерн
    скрытого редактирования, что и у строк блоков).

  **Инструкция для сервера:**
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php \
      frontend/views/goal/copy-confirm.php
  php yii cache/flush-all
  ```
  Затем — жёсткое обновление страницы в браузере (Ctrl+F5).

  Проверка: на карточке любого дня — «Скопировать в...» → выбрать дату без блоков →
  «Скопировать» → блоки дня-источника появились в дне-назначении. Повторить с датой,
  где уже есть блоки, — должна показаться страница подтверждения с количеством
  существующих блоков; после «Да, заменить» — старые блоки исчезают, вместо них копия.

- **12.09.2026 — Этап 4: альтернативные варианты дня + флажок «сделано»; версия
  v.1.7.1.1.** Перед разработкой уточнил у пользователя (по правилу «не гадать»):
  (1) варианты дня называются автонумерацией «Вариант 1/2/3» (без ввода текста
  пользователем); (2) переключение между вариантами — выпадающий список в шапке
  карточки дня; (3) если у дня-источника при «Скопировать в...» несколько вариантов —
  переносится только текущий (выбранный) вариант. Коммит
  [`6d5804e`](https://github.com/staschern/yavdele2/commit/6d5804e) в ветку
  `claude/yii2-project-setup-dsPYD`. Есть новая миграция.

  **Файлы:**
  - `console/migrations/m260912_090000_add_variants_and_done_to_schedule_block.php`
    (новый) — добавляет `schedule_block.variant_no` (по умолчанию 1 — старые блоки
    без миграции данных остаются в единственном варианте) и `schedule_block.is_done`;
    создаёт таблицу `schedule_day_variant` (только факт существования варианта, в т.ч.
    пустого, и какой из вариантов выбран для показа — пока у даты один вариант, строк
    для неё нет вообще).
  - `common/models/goal/ScheduleDayVariant.php` (новый) — модель +
    `getVariantNumbersForDate`/`getSelectedVariant`/`ensureVariantExists`/`selectVariant`.
  - `common/models/goal/ScheduleBlock.php` (изменён) — `variant_no`/`is_done` в
    правилах; `getBlocksForUserAndPeriod` возвращает эти поля; `getBlocksForDate`
    заменён на `getBlocksForDateAndVariant` (та же логика, плюс фильтр по варианту).
  - `frontend/controllers/GoalController.php` (изменён) — `actionWeek()`,
    `actionScheduleBlockSave()`, `actionScheduleBlockCopyDay()` стали
    variant-aware (показывают/редактируют/копируют только выбранный вариант
    даты); новые `actionScheduleBlockVariantAdd()` (создать вариант — пустой
    или копией текущего — и сделать выбранным), `actionScheduleBlockVariantSelect()`
    (сделать существующий вариант выбранным, сохраняется в БД) и
    `actionScheduleBlockToggleDone()` (быстрый флажок без входа в редактирование).
  - `frontend/views/goal/week.php` (изменён) — в шапке карточки дня: выпадающий
    список вариантов (если их больше одного) + ссылка «+ вариант дня» с выбором
    «начать с пустого»/«скопировать текущий вариант»; в каждой строке блока —
    первая колонка с иконкой-флажком (сразу сохраняет клик в БД, без открытия
    редактирования), название зачёркивается при отметке «сделано».
  - `frontend/views/layouts/main.php` (изменён) — версия в подвале `v.1.6.1.1` →
    `v.1.7.1.1` (по прямому указанию пользователя).

  **Версионирование:** `A.B.C.D` — `A` меняется только по указанию пользователя,
  `B` — номер триместра, `C` — новый механизм, `D` — новая функциональность в рамках
  механизма. Стоит напоминание себе: после каждой доработки уточнять у пользователя,
  не пора ли обновить версию, если он сам не поднял тему (в этот раз — прямо указал не
  менять дальше в рамках той же доработки).

  **Инструкция для сервера** (есть миграция БД):
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php \
      frontend/views/layouts/main.php common/models/goal/ScheduleBlock.php \
      common/models/goal/ScheduleDayVariant.php \
      console/migrations/m260912_090000_add_variants_and_done_to_schedule_block.php
  php yii migrate --interactive=0
  php yii cache/flush-all
  ```
  Затем — жёсткое обновление страницы в браузере (Ctrl+F5).

  Проверка: в подвале сайта — «v.1.7.1.1»; на карточке дня без второго варианта —
  просто «Вариант 1» без выпадающего списка; «+ вариант дня» → «начать с пустого» или
  «скопировать текущий» → «Создать» — появляется выпадающий список с двумя вариантами,
  открыт новый; переключение в списке между вариантами сохраняется и после обновления
  страницы (F5) остаётся на выбранном варианте, а не сбрасывается на первый; клик по
  флажку у блока сразу меняет иконку и зачёркивает название без входа в редактирование.

- **12.09.2026 — фикс: блокировка смены варианта дня во время редактирования записи.**
  Пользователь сообщил: «кнопка Сохранить перестала работать» при редактировании
  блока. По скриншоту и описанию — вероятная причина: выпадающий список вариантов
  (`onchange="this.form.submit()"`) и «+ вариант дня» находятся прямо над формой
  редактирования записи; случайный клик по ним мгновенно перезагружает страницу и
  теряет незасохранённые правки открытой записи — воспринимается как «Сохранить не
  работает». Пользователь сам сформулировал нужное поведение: при входе в
  редактирование записи блокировать смену варианта; менять вариант можно только
  тогда, когда ничего не редактируется. Коммит
  [`b60534e`](https://github.com/staschern/yavdele2/commit/b60534e) в ветку
  `claude/yii2-project-setup-dsPYD`. Только `week.php`, БД/миграции не менялись.

  **Файлы:**
  - `frontend/views/goal/week.php` (изменён) — `weekUpdateVariantLock()`: при
    открытии/закрытии любой формы редактирования (запись, добавление, копирование
    дня, создание варианта) блокирует все `<select>` выбора варианта (`disabled`) и
    гасит ссылки «+ вариант дня» (`pointer-events:none`) на всех карточках дня, пока
    открыта хоть одна форма редактирования; снимает блокировку, когда открытых форм
    не остаётся.

  **Инструкция для сервера:**
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  Затем — жёсткое обновление страницы в браузере (Ctrl+F5).

  Проверка: открыть редактирование любой записи — выпадающий список варианта и
  «+ вариант дня» должны стать неактивными (выцветшими/некликабельными) у всех трёх
  карточек дня; закрыть редактирование (Сохранить или Отмена) — управление вариантом
  снова доступно. Повторно проверить, что «Сохранить» в записи действительно
  сохраняет изменение.

- **12.09.2026 — критично: весь сайт ЯВД перестал отвечать, убраны SQL-слова из URL.**
  Пользователь сообщил: все страницы ЯВД перестали реагировать на нажатия, страницы
  бесконечно грузятся (не только «Неделя» — весь сайт). Собственная гипотеза
  пользователя: защита сервера от перебора URL блокирует запросы с словами,
  используемыми в SQL-запросах, — а в новых URL Этапа 4 были буквально `delete` и
  `select`. Похоже на срабатывание бана по IP/сессии на стороне хостинга (что
  объясняет эффект «весь сайт», а не только одна страница) — это НЕ чинится кодом,
  разблокировать может только хостинг-панель/провайдер. Со своей стороны убрал
  SQL-слова из новых URL, чтобы не провоцировать повторно. Коммит
  [`84ed156`](https://github.com/staschern/yavdele2/commit/84ed156) в ветку
  `claude/yii2-project-setup-dsPYD`. Только код (переименование методов/URL),
  логика действий не менялась, миграций нет.

  **Переименовано:**
  - `actionScheduleBlockDelete` → `actionScheduleBlockRemove`
    (`/goal/schedule-block-delete` → `/goal/schedule-block-remove`).
  - `actionScheduleBlockVariantAdd` → `actionScheduleBlockVariantCreate`
    (`/goal/schedule-block-variant-add` → `/goal/schedule-block-variant-create`).
  - `actionScheduleBlockVariantSelect` → `actionScheduleBlockVariantChoose`
    (`/goal/schedule-block-variant-select` → `/goal/schedule-block-variant-choose`).

  **Файлы:** `frontend/controllers/GoalController.php`, `frontend/views/goal/week.php`
  (обновлены ссылки форм на новые URL).

  **Пользователю нужно проверить на стороне хостинга** (я туда доступа не имею):
  панель управления сервером (ISPmanager/cPanel/аналог) → раздел
  «Безопасность»/«Firewall»/«Забаненные IP» — снять блокировку своего IP, если он
  там есть; также попробовать открыть сайт с другого устройства/сети (например,
  с мобильного интернета) — если там сайт открывается нормально, это подтвердит,
  что дело именно в бане по IP, а не в поломке самого сервера.

  **Инструкция для сервера (когда доступ восстановится):**
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php
  php yii cache/flush-all
  ```

  **Найдена точная причина (пользователь прислал код) — важное постоянное
  ограничение площадки на будущее.** В `vendor/yiisoft/yii2/web/Application.php`
  (не в гите — `/vendor` в `.gitignore`, правка видна только на живом сервере)
  живьём пропатчен метод `handleRequest()`: если полный URL (`absoluteUrl`, путь +
  query-строка) содержит одну из подстрок ниже (без учёта регистра), отдаётся 502 и
  **перед этим `sleep(30*60)` — 30 минут сна воркера**. При ограниченном пуле
  PHP-FPM даже нескольких попаданий по забаненному URL достаточно, чтобы забить ВСЕ
  воркеры сразу — отсюда «весь сайт не отвечает», а не только одна страница.

  **Список запрещённых подстрок (регистронезависимо):** `.php` `.xml` `.txt` `.env`
  `.bak` `.git` `config` `.gz` `.tar` `.rar` `.zip` `.js` `union` `select` `update`
  `exists` `when` `%27` `%29` `_schema`. Плюс отдельно блокируется User-Agent,
  содержащий `sqlmap`. (`delete` в коде есть, но сейчас закомментирован — не
  активен, но всё равно избегать на будущее, если снова включат.)

  **Правило на все дальнейшие доработки ЯВД:** перед выбором имени нового
  action/URL — проверять его (и весь путь `/goal/...`, включая параметры в query,
  не в POST-теле) на отсутствие этих подстрок как ЧАСТИ слова, не только целым
  словом (например, `updateProfile` → `update-profile` содержит `update` и будет
  заблокировано; `whenever` содержит `when`). Актуальная причина этого инцидента —
  `schedule-block-variant-select` (содержал `select`), уже переименован в
  `...-choose` в коммите `84ed156`; остальные текущие URL раздела «Неделя»
  проверены скриптом на полное отсутствие совпадений — чисты.

  **Немедленное восстановление доступа (сторона сервера, не гита):** самое быстрое —
  перезапустить PHP-FPM, чтобы разом снять зависшие в `sleep()` воркеры, а не ждать
  30 минут:
  ```bash
  systemctl list-units --type=service | grep -i php
  systemctl restart <точное_имя_сервиса_из_списка>
  ```

  **Итог расследования (диагностировано по логам БД, без гаданий):** ровно 2 попадания
  на старый URL `schedule-block-variant-select`, с разницей ровно 1800 сек
  (13:22:22 и 13:52:22) — одна и та же зависшая старая вкладка браузера, повторившая
  запрос сразу после того, как первая попытка истекла. После деплоя переименования и
  перезапуска php-fpm новых блокировок не было. **Финальная причина, почему сайт
  продолжал висеть именно на одном конкретном устройстве** — не сервер: в обычном
  (не инкогнито) профиле Chrome застряли кэш/service worker с той самой сломанной
  страницы. Через приватный режим сайт открылся сразу — это и указало на клиентскую
  причину. Помогла полная очистка данных сайта в браузере
  (`chrome://settings/content/all` → yavdele.com → «Очистить данные»). Инцидент закрыт,
  пользователь подтвердил — сохранение записи снова работает.

- **12.09.2026 — Этап 5: импорт недельного расписания из JSON от Артёма.**
  Реальный пример JSON для проектирования формата запросил у пользователя; тот
  обратился к Артёму («Конструктор дня»), который подготовил
  `../../1. Система и порядок/2026.09-12/расписание-2026.09.07-13.json` — фактическое
  расписание недели 07-13.09.2026, включая второй вариант субботы 12.09 без вылазки
  (пользователь решил не выбираться — холодно; работа с ЯВД продлена до 15:00, далее
  обед, уборка 1,5ч, проект, час «Образования»). На основе этого реального примера
  согласован и реализован формат: `{"days":[{"date":"YYYY-MM-DD","variants":[{"label":
  необязательно,"blocks":[{"start":"HH:MM","end":"HH:MM","title":"..."}]}]}]}`. Коммит
  [`05bcd77`](https://github.com/staschern/yavdele2/commit/05bcd77) в ветку
  `claude/yii2-project-setup-dsPYD`. Новых миграций нет — использует схему Этапа 4
  (`variant_no`).

  Ранее уточнил у пользователя (по правилу «не гадать»): (1) JSON загружается файлом
  (не текстовым полем); (2) при импорте текущий выбранный вариант дня заменяется
  (не дополняется); (3) импорт — сразу вся неделя одним файлом. Реальный пример от
  Артёма показал, что у одной даты может быть НЕСКОЛЬКО вариантов в одном JSON
  (суббота) — спроектировал естественное расширение решения пользователя: i-й по
  порядку вариант в JSON заменяет i-й вариант на сайте (создаёт, если не было),
  варианты сайта сверх присланных в JSON не трогаются, после импорта на каждую дату
  выбирается вариант 1.

  **Файлы:**
  - `frontend/controllers/GoalController.php` (изменён) — `actionScheduleWeekImport()`:
    разбирает загруженный JSON, для каждой даты и каждого её варианта мягко удаляет
    старые блоки этого варианта и вставляет новые из файла; невалидные записи
    пропускаются, а не роняют импорт целиком.
  - `frontend/views/goal/import-error.php` (новый) — страница с перечнем ошибок
    разбора файла и ссылкой назад на неделю (используется, если импортировать
    нечего — например, файл не JSON или нет ни одной корректной даты).
  - `frontend/views/goal/week.php` (изменён) — над сеткой дней добавлена ссылка
    «Импорт JSON недели от Артёма», раскрывающая форму загрузки файла (тот же
    паттерн скрытого редактирования, что и у остальных форм на странице).

  Имя нового URL (`schedule-week-import`) заранее проверено на отсутствие слов,
  блокируемых защитой сервера (см. инцидент того же дня — `sleep(30 мин)` на
  `select`/`update`/`_schema` и т.д.).

  **Инструкция для сервера:**
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php \
      frontend/views/goal/import-error.php
  php yii cache/flush-all
  ```
  Затем — жёсткое обновление страницы в браузере (Ctrl+F5).

  Проверка: на странице «Неделя» — «Импорт JSON недели от Артёма» → выбрать файл
  `расписание-2026.09.07-13.json` (прислан пользователю отдельно) → «Импортировать» →
  должна открыться неделя с этим расписанием на всех 7 днях; у субботы 12.09 должно
  появиться 2 варианта в выпадающем списке (вариант 1 — с вылазкой/показан по
  умолчанию, вариант 2 — без вылазки, с уборкой и продлённой работой с ЯВД).

  **Пользователь проверил на бою — сработало.** По итогам два отзыва: (1) на сайте не
  должно быть личного имени агента («от Артёма») — убрал, осталось просто «Импорт JSON
  недели»; (2) нативная кнопка выбора файла просил стилизовать под сайт — заменена на
  `window-button-in-panel window-border` с подписью выбранного имени файла рядом.
  Заодно попросил симметричный **экспорт** — выгрузку текущей недели в тот же JSON.
  Коммит [`c9e26b8`](https://github.com/staschern/yavdele2/commit/c9e26b8) в ветку
  `claude/yii2-project-setup-dsPYD`. Только код, миграций нет.

  **Файлы:**
  - `frontend/controllers/GoalController.php` (изменён) — новый
    `actionScheduleWeekExport()`: выгружает неделю (Пн-Вс, содержащую `?n=`) в JSON
    того же формата, что принимает импорт, включая ВСЕ варианты каждого дня (не
    только выбранный) — файл можно сразу загрузить обратно, получается полный
    цикл экспорт→импорт.
  - `frontend/views/goal/week.php` (изменён) — подпись без личного имени, стилизованная
    кнопка выбора файла, ссылка «Экспорт JSON недели» рядом с импортом.

  **Инструкция для сервера:**
  ```bash
  cd /var/www/www-root/data/www/yavdele.com
  git fetch origin claude/yii2-project-setup-dsPYD
  git reset origin/claude/yii2-project-setup-dsPYD
  git checkout -- frontend/controllers/GoalController.php frontend/views/goal/week.php
  php yii cache/flush-all
  ```
  Затем — жёсткое обновление страницы в браузере (Ctrl+F5).

  Проверка: кнопка выбора файла — в стиле остальных кнопок сайта; «Экспорт JSON
  недели» скачивает файл с текущей неделей; повторная загрузка этого же файла через
  «Импорт» должна восстановить ту же неделю без потерь (включая оба варианта субботы).

## Сводка прогресса (раз в месяц)

_Первая сводка — через месяц после старта работы с агентом._
